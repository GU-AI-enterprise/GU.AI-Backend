export enum AssetType {
  IMAGE = 'image',
  VIDEO = 'video',
  FILE = 'file',
}

export enum AssetCategory {
  PRODUCT = 'product',
  MODEL = 'model',
  BACKGROUND = 'background',
  OUTPUT = 'output',
  REFERENCE = 'reference',
}

export enum AssetRole {
  INPUT = 'input',
  OUTPUT = 'output',
  PRODUCT = 'product',
  MODEL = 'model',
  BACKGROUND = 'background',
  REFERENCE = 'reference',
  MASK = 'mask',
}
